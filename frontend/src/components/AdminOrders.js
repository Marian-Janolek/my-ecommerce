import React from 'react';
import styled from 'styled-components';

const AdminOrders = () => {
  return <Wrapper>admin orders</Wrapper>;
};

const Wrapper = styled.article``;

export default AdminOrders;
