import React from 'react';
import styled from 'styled-components';
import { AmountButtons } from '.';

const AddToCart = () => {
  return (
    <Wrapper>
      <div className="btn-container">{/* <AmountButtons /> */}</div>
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default AddToCart;
