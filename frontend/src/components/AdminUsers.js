import React from 'react';
import styled from 'styled-components';

const AdminUsers = () => {
  return <Wrapper>user list</Wrapper>;
};

const Wrapper = styled.article``;

export default AdminUsers;
