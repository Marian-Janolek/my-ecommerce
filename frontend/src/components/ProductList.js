import React from 'react';
import { GridView, ListView } from '.';

const ProductList = () => {
  return (
    <div>
      <GridView />
      <ListView />
    </div>
  );
};

export default ProductList;
