import React from 'react';
import styled from 'styled-components';

const UserUpdate = () => {
  return <Wrapper>update user</Wrapper>;
};

const Wrapper = styled.section``;

export default UserUpdate;
