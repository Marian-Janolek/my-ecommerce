import React from 'react';
import styled from 'styled-components';

const UserReviews = () => {
  return <Wrapper>user reviews</Wrapper>;
};

const Wrapper = styled.div``;

export default UserReviews;
