import React from 'react';
import styled from 'styled-components';

const ProfileMenu = () => {
  return <Wrapper>profile menu</Wrapper>;
};

const Wrapper = styled.div``;

export default ProfileMenu;
