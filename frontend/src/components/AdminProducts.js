import React from 'react';
import styled from 'styled-components';

const AdminProducts = () => {
  return <Wrapper>product list</Wrapper>;
};

const Wrapper = styled.article``;

export default AdminProducts;
