import {
  ADD_TO_CART,
  CLEAR_CART,
  REMOVE_CART_ITEM,
  COUNT_CART_TOTALS,
  TOGGLE_CART_ITEM_AMOUNT,
} from '../actions';

const cartReducer = (state, action) => {};

export default cartReducer;
